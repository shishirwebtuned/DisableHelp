deployment:
  tasks:
    - export PATH=/opt/cpanel/ea-nodejs18/bin:$PATH
    - cd /home/go0glecom/DisableHelpNew/backend
    - npm install
    - npm run build
    - mkdir -p tmp
    - touch tmp/restart.txt
